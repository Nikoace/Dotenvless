# M7：无值环境变量示例

dvl example 根据当前项目键名，在 Git 根目录创建 .env.example。内容为排序后的 KEY= 行，UTF-8，无真实值；没有键时生成空文件。

默认拒绝覆盖现有文件，不跟随符号链接。通过排他创建保证已存在文件/目录/链接不会被覆盖；写入自身新文件失败时清理该文件。V0.1 不添加强制覆盖选项，用户可自行合并已有示例。

验收：子目录调用仍写入根；键排序/空值；不含测试假 Secret；已有内容逐字节保留；不创建真实 .env。
