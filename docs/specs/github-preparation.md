# GitHub 首次提交准备

状态：首次工程整理已完成。用户已确认创建公开仓库并清理历史后推送，新增验收见 GH-09；仍暂不添加许可证、不发布 Release。

| ID | 验收 |
| --- | --- |
| GH-01 | README 从新克隆用户视角说明 Windows/Go/Git 要求、构建、使用和限制；不依赖作者机器路径或已下载工具链。 |
| GH-02 | Windows CI 从 go.mod 读取 Go 版本，运行格式、依赖校验、vet、测试、构建和二进制帮助检查；仅上传生成的 dvl.exe。 |
| GH-03 | 提供贡献说明、变更日志、Issue/PR 模板与私密漏洞报告指引；不声明已获得开源许可证。 |
| GH-04 | 多行单/双引号值保留首行末尾空白，含 export 的语法亦然；未加引号值保持既有空白清理行为。 |
| GH-05 | .env.[dev] 等文件按字面量查询 Git 状态，不能因 .env.d 被跟踪而误报；ignored/tracked 状态仍正确。 |
| GH-06 | 检查当前索引与可达 Git 历史：工具链、构建产物、Vault、真实环境文件不在提交中；保留验收历史，不改写已有提交。 |
| GH-07 | 完成格式、vet、全量测试及 Windows 构建，并从仅含版本文件的独立副本验证构建；main 快进到最终提交，工作区干净。 |
| GH-08 | 当前 README、文档与 GitHub 模板不包含本机绝对路径、账户名或专属缓存目录；示例采用仓库相对路径或平台环境变量，文档链接有效，历史验收结果保留。 |
| GH-09 | 用户确认公开发布后，创建公开仓库；本地备份原历史，清理历史中的本机信息，保留提交数量与代码内容；仅推送清理后的 main，核对公开状态、默认分支和远端提交。 |
| GH-10 | 配置与子进程测试按物理目录身份验证路径别名；确认 Vault 目录与 APPDATA 为同一目录、后缀为 dotenvless/vault.dat，且解析不创建文件；子进程 cwd 指向父进程工作目录。Windows CI 完成测试、构建和产物上传。 |

行为修复沿用 SDD/TDD，真实 Red/Green 记录在 docs/verification.md；模板和文档采用结构/链接与实际构建验证，不增加只检查文案的单元测试。GitHub Actions 必须在用户推送后才能获得远程执行证据。
